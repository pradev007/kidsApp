import 'package:flutter/material.dart';

class LearnAnimalScreen extends StatelessWidget {
  const LearnAnimalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Animal Page'),
      ),
    );
  }
}
