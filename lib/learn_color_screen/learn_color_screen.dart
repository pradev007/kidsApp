import 'package:flutter/material.dart';

class LearnColorScreen extends StatelessWidget {
  const LearnColorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Color Page'),
      ),
    );
  }
}
